<%= stylesheet_link_tag 'calendar/red/style' %>

<%=

#calendar(calendar_options) do |d|
calendar({ :year => @display_date.year, 
			  :month => @display_date.month, 
			  :first_day_of_week => 0 })

%>